<div class="container">
    <%= link_to "Add Cocktail", new_cocktail_path, class: "btn btn-primary" %>
    <ul>
        <% @cocktails.each do |cocktail| %>
            <li><%= cocktail %></li>
        <% end %>
    </ul>
</div>
